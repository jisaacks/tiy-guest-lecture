# Application.addRegions

```javascript
var App = new Backbone.Marionette.Application();

App.addRegions({
  header: "#header",
  content: "#content",
  footer: "#footer"
});
```

[Next](D%20getRegion.md)